// List of Enums
// They cannot be created a class.

enum TextSizes {small, medium, large}

enum OrderStatus {processing, shipped, delivered}

enum PaymentMethods {paypal, googlePay, applePay, masterCard, creditCard, paystack, razorPay, paytm}